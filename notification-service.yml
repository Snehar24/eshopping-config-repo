# ===================================================================
# EShoppingZone - Notification Service Centralized Configuration
# ===================================================================
server:
  port: 8092

spring:
  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USER:guest}
    password: ${RABBITMQ_PASSWORD:guest}
  mail:
    host: smtp.gmail.com
    port: 587
    username: sneharamesh201204@gmail.com
    password: trld ujjf xmuq phyc
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

info:
  app:
    name: "Notification Service"
    description: "EShoppingZone Email & Alert Notification Microservice"
    version: "1.0.0"
