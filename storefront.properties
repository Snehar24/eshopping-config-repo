# ===================================================================
# EShoppingZone - Storefront Centralized Configuration
# ===================================================================
server.port=8080

spring.datasource.url=jdbc:h2:file:./data/eshoppingzone;MODE=MySQL;AUTO_SERVER=TRUE
spring.datasource.username=sa
spring.datasource.password=

spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false

spring.thymeleaf.cache=false

spring.rabbitmq.host=${RABBITMQ_HOST:localhost}
spring.rabbitmq.port=${RABBITMQ_PORT:5672}
spring.rabbitmq.username=${RABBITMQ_USER:guest}
spring.rabbitmq.password=${RABBITMQ_PASSWORD:guest}
spring.rabbitmq.listener.simple.auto-startup=${RABBITMQ_AUTO_STARTUP:false}

spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:sneharamesh201204@gmail.com}
spring.mail.password=${MAIL_PASSWORD:trld ujjf xmuq phyc}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Storefront Circuit Breaker Configuration
resilience4j.circuitbreaker.instances.notificationService.base-config=default
resilience4j.circuitbreaker.instances.openAiImageService.base-config=default

info.app.name=EShoppingZone Storefront
info.app.description=Storefront Web Application
info.app.version=1.0.0
